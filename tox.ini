[tox]
envlist =
    py3
isolated_build = True

[testenv]
deps =
    pandas
    pytest
    pytest-cov
    pytest-codeblocks
    plotly
    pytest-randomly
    kaleido<1.0.0
commands =
    pytest {posargs}